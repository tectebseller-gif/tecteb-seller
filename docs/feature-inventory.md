# فهرست امکانات و وضعیت ساخت

آخرین به‌روزرسانی: ۱۴ سپتامبر ۲۰۲۶ · بستهٔ جاری: `0.1.0-alpha.5`

این فهرست هر نوبت تحویل به‌روز می‌شود. سه وضعیت، با معنای دقیق:

| وضعیت | یعنی |
|---|---|
| **ساخته‌شده** | مسیر عملی کامل کار می‌کند، آزمون دارد، و شواهدش ثبت شده. صفحه یا دکمهٔ تنها «ساخته‌شده» نیست |
| **ناقص** | بخشی کار می‌کند و بخشی نه؛ ستون «چه چیزی کم است» دقیقاً می‌گوید کدام |
| **باقی‌مانده** | هنوز شروع نشده |
| **مسدود** | ناقص، و علتش بیرون از کنترل ماست (بند ۶) |

---

## ۰. تطبیق ترتیب ساخت شما با اسناد مصوب

ترتیب پنج‌گانهٔ شما با گیت‌های فاز سند مادر (بند ۲۶) تطبیق داده شد:

| ترتیب شما | گیت سند مادر | تطبیق |
|---|---|---|
| ۱. تکمیل فروشندگان: پروفایل، تنظیمات فروشگاه، پرسنل و نقش‌ها، دعوت، دسترسی‌ها | گیت فاز ۳ «staff و دسترسی» + طرح ۲-ج و ۲-چ | ✅ منطبق |
| ۲. محصولات: ثبت/ویرایش، فیلدهای پویای دسته، قیمت، موجودی، تصاویر، بررسی مدیر | گیت فاز ۴ «محصولات/تأیید/فرم پزشکی/CSV» | ✅ منطبق (CSV هم در همان گیت است و به فهرست اضافه شد) |
| ۳. سفارش و ارسال | گیت فاز ۵ | ⚠️ **یک تفاوت — بند زیر** |
| ۴. کمیسیون، دفترکل، تسویه | گیت فاز ۵ و ۶ | ⚠️ همان تفاوت |
| ۵. یکپارچه‌سازی داشبورد و اعلان‌ها، سپس مهاجرت آزمایشی از دکان | گیت فاز ۷ و ۸ | ✅ منطبق |

> **تفاوت واقعی و تنها تفاوت:** سند مادر بند ۲۶ می‌گوید
> «**ترتیب قرارداد مالی پیش از عملیات سفارش تکمیل شود**» و دفترکل را در همان
> گیت سفارش می‌آورد. یعنی مرحلهٔ ۴ شما (کمیسیون و دفترکل) باید **پیش از
> عملیاتی‌شدن** مرحلهٔ ۳ (سفارش) بسته شود، وگرنه سفارشی ثبت می‌شود که سهم
> مالی‌اش هیچ‌جا نوشته نشده و بازگشت از آن ممکن نیست.
>
> **کاری که می‌کنیم و اجازه نمی‌خواهد:** مرحلهٔ ۳ را به دو نیم می‌کنیم —
> «دسترسی فروشنده به سفارش‌های خودش، وضعیت‌ها و ارسال» که مالی نیست و طبق
> ترتیب شما جلو می‌رود؛ و «ثبت سهم مالی هر سفارش» که با مرحلهٔ ۴ می‌آید و تا
> آن زمان **هیچ عددی نمایش داده نمی‌شود** (نه صفر، نه تخمین). اگر ترجیح
> می‌دهید کل مرحلهٔ ۳ پس از ۴ بیاید، بگویید؛ بدون پاسخ شما همین تقسیم اجرا
> می‌شود.

---

## ۱. فاز ۱ — زیرساخت و چهار صفحهٔ مدیریت

| قابلیت | وضعیت | چه چیزی کم است |
|---|---|---|
| Autoloader، container، بارگذاری دومرحله‌ای ماژول‌ها | **ساخته‌شده** | — |
| Migration با قفل مالک‌دار، ازسرگیری، UpgradeGate | **ساخته‌شده** | — |
| تنظیمات (کمیسیون، تسویه، سقف پرسنل، محیط) | **ساخته‌شده** | — |
| لاگ ممیزی با allowlist رویداد و payload پاک‌شده | **ساخته‌شده** | — |
| چهار صفحهٔ مدیریت: پیشخوان، سلامت، تنظیمات، ماژول‌ها | **ساخته‌شده** | — |
| REST سلامت با capability و nonce | **ساخته‌شده** | — |
| گارد محیط و قفل ارسال Alpha | **ساخته‌شده** | — |
| چرخهٔ فعال‌سازی/غیرفعال‌سازی/حذف بدون حذف داده | **ساخته‌شده** | — |
| بسته‌بندی تکرارپذیر با هش ثابت و ضد بازنویسی نسخه | **ساخته‌شده** | — |

## ۲. فاز ۲ — فروشنده: درخواست، مدارک، بررسی، پیشخوان

| قابلیت | وضعیت | چه چیزی کم است |
|---|---|---|
| فرم درخواست، پیش‌نویس، ماشین وضعیت هفت‌حالته | **ساخته‌شده** | — |
| فرم‌ساز مدارک مدیر + سه وضعیت الزامات | **ساخته‌شده** | — |
| بارگذاری مدرک با تشخیص نوع واقعی و سقف حجم | **ساخته‌شده** | — |
| ذخیرهٔ مدارک بیرون از ریشه‌های وب + توقف روشن اگر محل امن نباشد | **ساخته‌شده** | — |
| صف بررسی مدیر، تأیید/اصلاح/رد با یادداشت و audit | **ساخته‌شده** | — |
| پیشخوان فروشنده روی مسیر `/vendor/` با پوستهٔ مستقل | **ساخته‌شده** | — |
| دو مجوز جدا: اجازهٔ فروش و اجازهٔ انتشار مستقیم | **ساخته‌شده** | — |
| **تعلیق فروشنده** (حالت `suspended` در ماشین وضعیت هست) | **ناقص** | رابط مدیر برای تعلیق و بازگردانی، و ابطال فوری دسترسی‌های جاری |
| ثبت موبایل بدون ادعای تأیید | **ساخته‌شده** | — |
| تأیید واقعی موبایل با OTP | **مسدود** | `tecteb-gateway-reference.zip` (بند ۶-۱) |

## ۳. مرحلهٔ ۱ شما — تکمیل فروشندگان

| قابلیت | مرجع | وضعیت | چه چیزی کم است |
|---|---|---|---|
| تنظیمات فروشگاه — زبانهٔ عمومی (نام، شهر، معرفی، لوگو، بنر) | UX ۱۱ | **ساخته‌شده** | — |
| تغییر نام فروشگاه/نام حقوقی ⇒ درخواست تغییر و تأیید مدیر | UX ۱۱ | **ساخته‌شده** | — |
| زبانهٔ ارسال (زمان آماده‌سازی، انبار مبدأ، شرکت‌های مجاز) | UX ۱۱ | **ناقص** | فهرست شرکت‌های حمل را مدیر باید وارد کند؛ سازوکارش ساخته شده، فهرستش حدس زده نمی‌شود (بند ۶-۳) |
| زبانهٔ تعطیلی (فوری/زمان‌دار، پیام بازگشایی) | UX ۱۱ | **ساخته‌شده** | — |
| زبانهٔ اجتماعی با شبکه‌های مجاز مدیر و اعتبارسنجی URL | UX ۱۱ | **ساخته‌شده** | — |
| زبانهٔ بانکی: شبا، صاحب حساب، مدرک، فقط مالک، تأیید مدیر | UX ۱۱ · A.4 | **ناقص** | مرحلهٔ OTP انجام نمی‌شود (بند ۶-۱). «توقف تسویه» پس از تغییر شبا ثبت می‌شود ولی تسویه‌ای هنوز وجود ندارد که متوقف شود |
| فهرست پرسنل با سقف، وضعیت و آخرین فعالیت | UX ۹٫۱ | **ساخته‌شده** | — |
| افزودن پرسنل (نام، نام خانوادگی، نام کاربری، ایمیل، موبایل) | UX ۹٫۱ | **ساخته‌شده** | — |
| پنج نقش آماده + مجوز سفارشی روی پنج حوزه | مادر ۳٫۱ | **ساخته‌شده** | — |
| دعوت پرسنل | UX ۹٫۱ | **ناقص** | لینک یک‌بارمصرف ساخته می‌شود و فروشنده خودش تحویل می‌دهد؛ **ارسال خودکار پیامک/ایمیل در Alpha بسته است** (بند ۶-۱) |
| تعلیق فوری پرسنل بدون حذف تاریخچه | مادر ۳٫۱ | **ساخته‌شده** | — |
| عدم دسترسی متقاطع بین فروشگاه‌ها و عدم self-enrollment | AC-PRIV | **ساخته‌شده** | — |
| سقف پرسنل از تنظیمات مدیر (`max_staff`) | A.4 | **ساخته‌شده** | — |
| گزارش فعالیت پرسنل | مادر ۵ | **ناقص** | فقط «آخرین فعالیت» ثبت می‌شود؛ گزارش تفصیلی با فاز ۷ می‌آید |

## ۴. مرحله‌های بعدی — هنوز شروع نشده

| مرحله | قابلیت‌های اصلی | وضعیت |
|---|---|---|
| ۲ محصولات | ثبت/ویرایش، قیمت، موجودی، تصاویر، دسته/برند، **فیلدهای پویای پزشکی دسته‌محور**، بررسی و تأیید مدیر، ورود/خروج CSV | **باقی‌مانده** |
| ۳ سفارش و ارسال | دسترسی فروشنده فقط به سفارش‌های خودش، وضعیت‌ها، روش‌های ارسال، رهگیری، واگذاری به پرسنل، سبد چندفروشنده | **باقی‌مانده** |
| ۴ مالی | قانون کمیسیون + snapshot، دفترکل تغییرناپذیر، تسویه و برداشت، مرجوعی | **باقی‌مانده** — و بند ۶-۲ باید پیش از شروعش بسته شود |
| ۵ یکپارچه‌سازی | داشبورد یکپارچه، اعلان‌ها، تیکت، نظرات، کوپن، B2B، SEO، گزارش | **باقی‌مانده** |
| ۵ مهاجرت | importer فقط‌خواندنی از دکان، نگاشت، dry-run، reconciliation، rehearsal بازگشت | **باقی‌مانده** |

## ۵. اصلاحات ظاهری کوچک که نگه داشته شده‌اند

| # | مورد | وضعیت |
|---|---|---|
| ظ-۱ | متن خطای حجم «حداکثر ۰ مگابایت» | **انجام شد** در `alpha.4` |
| ظ-۲ | فهرست خالی «فرمت‌های مجاز:» و «مدارک ناقص:» | **انجام شد** در `alpha.4` |
| ظ-۳ | نمایش محل نگهداری مدارک به مدیر | **انجام شد** در `alpha.5` |
| ظ-۴ | شمارهٔ صفحه/مرتب‌سازی در فهرست درخواست‌های مدیر | **باقی‌مانده** — با بیش از چند ده درخواست لازم می‌شود |
| ظ-۵ | نمایش تاریخ‌ها به شمسی در پیشخوان فروشنده | **باقی‌مانده** — امروز میلادی نمایش داده می‌شود |
| ظ-۶ | پیام خالی‌بودن فهرست پرسنل برای فروشندهٔ تازه | **انجام شد** در `alpha.5` |

## ۶. ابهام‌های مسدودکننده — یک‌جا، و فقط همان قابلیت وابسته متوقف است

| # | ابهام | مرجع | چه چیزی را مسدود می‌کند | چه چیزی را مسدود **نمی‌کند** |
|---|---|---|---|---|
| ۱ | **فایل `tecteb-gateway-reference.zip`** — هنوز در مخزن نیست | طرح ۴٫۳ · A.4 | تأیید موبایل، ورود با OTP، ارسال خودکار دعوت پرسنل، مرحلهٔ OTP زبانهٔ بانکی | هیچ‌چیز دیگر. پرسنل با لینک دستی دعوت می‌شود و شبا با تأیید مدیر ثبت می‌شود |
| ۲ | **قواعد مالی**: نرخ کمیسیون پایه و استثناها، تعریف «سفارش تکمیل‌شده» (DEC-02/FIN-04)، مدت مرجوعی (DEC-03) | مادر ۲۷ · دفتر تصمیم‌ها | کل مرحلهٔ ۴ (کمیسیون، دفترکل، تسویه) و نمایش هر عدد مالی در سفارش | ثبت سفارش، وضعیت‌ها و ارسال — به شرط اینکه هیچ عدد مالی نمایش ندهیم |
| ۳ | **فهرست شرکت‌های حمل مجاز** و روش‌های ارسال | UX ۱۱ · مادر ۴ | انتخاب شرکت حمل توسط فروشنده | زبانهٔ ارسال (زمان آماده‌سازی و انبار مبدأ کار می‌کنند) |
| ۴ | **فهرست واقعی مدارک** هر نوع فروشنده و دسته | A.4 | فقط ارسال درخواست تا وقتی مدیر فهرست را وارد کند | زیرساخت، پیش‌نویس و صف بررسی |
| ۵ | **DEC-01 — موبایل حمل**: آیا شمارهٔ مشتری به فروشنده/پیک داده می‌شود؟ | مادر ۲۷ · PRIV-01 | نمایش اطلاعات تماس گیرنده در مرحلهٔ ۳ | بقیهٔ مرحلهٔ ۳ |
| ۶ | **فیلدهای پزشکی هر دستهٔ کالا** (IRC، UDI، شرایط نگهداری، …) | مادر ۴ | فهرست فیلدها | فرم‌ساز فیلد پویا — همان الگوی مدارک: سازوکار جدا از فهرست |

پاسخ‌های لازم را هر وقت رساندید، همان قابلیت باز می‌شود؛ بقیهٔ ساخت منتظر
نمی‌ماند.
