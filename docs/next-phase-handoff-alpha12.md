# تحویل به فاز بعد

**فاز ۲ خودکار شروع نمی‌شود.** این سند وضعیت واقعی پایان فاز ۱ است، نه مجوز
ادامه.

## ۱. وضعیت واقعی فاز ۱

| گیت فاز ۱ (سند مادر §۲۶) | وضعیت |
|---|---|
| bootstrap، Admin/Health، config، lifecycle | **ساخته و آزموده شد** در سطوح unit / معماری / stub / دیتابیس |
| «آزمون واقعی نصب» | **Passed** روی یک WordPress یکبارمصرف — WP 7.1 · WC 11.0.1 · PHP 8.1.32 و 8.4.19 · `docs/evidence/acceptance/` |
| رابط روی wp-admin واقعی (۵ عرض، زوم ۲۰۰٪، کیبورد، axe) | **Passed** — دو اجرا، هرکدام ۱۸۸ بررسی، ۰ شکست |
| نصب روی سایت تک‌طب | **انجام نشده** — خارج از مجوز فعلی |
| PHP 8.1.34 دقیقِ سایت، سرور وب واقعی، افزونه‌های سایت، screen reader دستی | **Not Run** — بند ۳٫۵ `docs/phase-1-report.md` |

گیت نصب فاز ۱ روی محیط یکبارمصرف **بسته شد**؛ برچسب ZIP همین را می‌گوید و
هم‌زمان تصریح می‌کند که روی سایت مالک نصب نشده است. سطرهای `Not Run` بالا
بسته نشده‌اند و با اجرای همان پروتکل در محیط مالک بسته می‌شوند.

## ۲. آنچه فاز ۲ باید پیش از شروع تعیین کند

| # | مورد | چرا مسدودکننده است |
|---|---|---|
| ۱ | **تبار افزونه (DEC-06-d)** | نسخه انتشار، برنامه سازگاری و هر ادعای «قابل نصب کنار نصب فعلی» به بررسی فقط‌خواندنی `tecteb-marketplace-v0.1.1.zip` وابسته است. هیچ گزینه‌ای انتخاب نشده |
| ۲ | **بستن گیت نصب** | تا اجرای `docs/installation.md` بند ۴ و برگشت خروجی، سازگاری ادعا نمی‌شود |
| ۳ | **DEC-01** موبایل حمل‌کننده | UI سفارش فاز ۵ بدون آن قابل طراحی نیست |
| ۴ | **DEC-02** تعریف «تکمیل» | آزادسازی مانده بدون آن ساخته نمی‌شود |
| ۵ | **DEC-04** مالیات/کوپن/گردکردن | پیش‌نیاز ماژول مالی |

## ۳. بدهی‌های باز که فاز ۱ عمداً نساخت

| بدهی | مرجع | یادداشت |
|---|---|---|
| retention و anonymization لاگ ممیزی | DEC-05، SEC-03 | جدول ساخته شد؛ **هیچ** cron، حذف خودکار یا exporter ساخته نشد. append-only به معنی نگهداری ابدی داده شخصی نیست |
| نسخه انتشار قطعی | F-01 | `0.1.0-alpha.1` با `TODO(F-01)` در main file علامت خورده است |
| سطح hook عمومی | `public-contracts.md` §۶ | عمداً خالی؛ با طراحی در فاز بعد معرفی شود |
| اجرای واقعی روی PHP 8.1 | `compatibility-matrix.md` | فقط شاهد ایستا داریم |
| screen reader دستی | UX §۲۴ | axe جای بررسی دستی را نمی‌گیرد |
| تداخل با LiteSpeed / Hello Elementor / Persian Woo / Rank Math / WP Rocket | DEC-06-b/c | هیچ‌کدام در دسترس نبودند |

## ۴. آنچه فاز ۲ می‌تواند مستقیم روی آن بنا کند

| قابلیت آماده | محل | نکته برای مصرف‌کننده بعدی |
|---|---|---|
| بارگذار ماژول با مهار خرابی | `Core/Modules/` | ماژول جدید فقط manifest و دو متد لازم دارد؛ وابستگی خراب خودکار مسدود می‌کند |
| migration مرحله‌ای + قفل مالک‌دار | `Core/Migration/` | مرحله جدید باید ایدمپوتنت باشد و `verify()` واقعی داشته باشد؛ `SchemaVersion::TARGET` را یک واحد بالا ببرید |
| تنظیمات با اعتبارسنجی و audit | `Core/Config/`, `Modules/Admin/` | کلید جدید را در `SettingsSchema` و `Settings` اضافه کنید؛ الگوی «کلید فرم ≠ کلید ذخیره‌شده» را برای هر مقدار مقیاس‌دار تکرار کنید |
| لاگ ممیزی | `Core/Audit/` | رویداد جدید **باید** در `AuditEventCatalog` allowlist شود، وگرنه ثبت نمی‌شود |
| قرارداد OTP | `Contracts/Otp/` | تنها پیاده‌سازی ارسالی NullProvider است؛ Adapter دروازه فقط با مستند API عمومی |
| قفل خروجی | `Core/Environment/OutboundPolicy.php` | هر sender آینده **باید** اول `assertAllowed()` را صدا بزند؛ الان همیشه استثنا می‌دهد |
| اجزای رابط فارسی/RTL | `Modules/Admin/Presentation/` | Button/Field/ErrorSummary/StatusBadge/DataList/Notice موجودند و کنتراستشان محاسبه شده |
| زیرساخت آزمون | `tests/`, `tools/` | چهار سطح شاهد جدا؛ لطفاً مخلوطشان نکنید |

## ۵. قواعدی که فاز ۲ نباید نقض کند

1. `Core`, `Contracts` و `Application` هیچ‌وقت WordPress صدا نمی‌زنند —
   `tests/Architecture` این را می‌گیرد.
2. هیچ رویداد audit خارج از allowlist؛ هیچ raw request در لاگ.
3. `null` هرگز به `false` تبدیل نمی‌شود؛ «نامشخص» یک وضعیت واقعی است.
4. هیچ ماژول `planned` دکمه فعال‌سازی نمی‌گیرد.
5. خروجی TMC تا تصمیم صریح مالک بسته می‌ماند.
6. `Not Run` هرگز بدون log قابل بازتولید به `Passed` تبدیل نمی‌شود.
7. تصاویر خارج از WordPress «نمونه رابط»اند، نه اثبات کارکرد.
8. اعتبارسنجی، ذخیره و ممیزی سه رویداد جدا هستند: هیچ ردیف ممیزی پیش از
   تأیید ذخیره نوشته نمی‌شود و هیچ پیام موفقیتی برای نوشتن انجام‌نشده.
9. اجرای منسوخ‌شده migration حق تغییر نسخه یا وضعیت اجرای جدید را ندارد؛ هر
   مرحله جدید باید ایدمپوتنت باشد وگرنه به ADR جدا نیاز دارد (ADR-003 §۳٫۱).
10. `stored > target` هرگز مهاجرت نمی‌شود و هرگز Healthy اعلام نمی‌شود.

## ۶. آنچه از این فهرست اجرا شد، و آنچه نماند

| مورد | وضعیت |
|---|---|
| suite integration واقعی WordPress (جایگزین سطح stub، نه حذف آن — ADR-005) | **اجرا شد** به‌صورت پروتکل گیت‌ها؛ suite خودکار integration همچنان نوشته نشده |
| ماتریس HPOS | **اجرا شد** — سه حالت `1/1/1`، `1/0/1`، `0/0/1` |
| اجرای کامل آزمون‌ها روی PHP 8.1 | **اجرا شد** روی ۸٫۱٫۳۲ ساخته‌شده از سورس؛ روی ۸٫۱٫۳۴ دقیقِ سایت `Not Run` |
| بررسی رابط روی wp-admin واقعی در پنج عرض و zoom ۲۰۰٪ | **اجرا شد** — `tools/browser/check-wpadmin.mjs` |

بازتولید: `tools/acceptance-gates.sh <php-binary> <evidence-dir> <package.zip>`
و `node tools/browser/check-wpadmin.mjs`.

## ۷. طرح اجرایی بخش فروشندگان (۱۱ سپتامبر ۲۰۲۶)

`docs/phase-2-vendor-plan.md` — ثبت‌نام و تأیید فروشنده، پروفایل و تنظیمات
فروشگاه، نقش‌ها و پرسنل، موبایل/ایمیل اجباری و اتصال OTP از قرارداد مستقل به
ورود موجود، به‌علاوه پیشخوان اولیه. همه بندها با ارجاع به سند مادر و UX، و
ابهام‌ها با تفکیک «مانع کدام مرحله است».

**طرح است؛ پیاده‌سازی شروع نشده** و تا پذیرش طراحی شروع نمی‌شود.
